inherits = "tokyonight_moon"

# 1. Main code window remains fully transparent for maximum acrylic effect
"ui.background" = { bg = "none" }
#"ui.linenr" = { bg = "none" }
#"ui.linenr.selected" = { bg = "none" }

# 2. Add dark, semi-transparent opacity to floating menus and popups (80% opacity)
#"ui.menu" = { bg = "#1f23351a" }
#"ui.menu.selected" = { bg = "#2e3c641a" }
#"ui.popup" = { bg = "#1f23351a" }
#"ui.help" = { bg = "#1f23351a" }

# 3. Style the bottom statusline bar (Normal mode, file name, selection info)
#"ui.statusline" = { bg = "#1f23351a" }          # Inactive/Standard background
#"ui.statusline.normal" = { bg = "#3b42611a" }   # Active Normal mode block
#"ui.statusline.insert" = { bg = "#3b42611a" }   # Active Insert mode block
#"ui.statusline.select" = { bg = "#3b42611a" }   # Active Select mode block
#"ui.statusline.inactive" = { bg = "#1f23350d" } # Fully blurred background when unfocused
